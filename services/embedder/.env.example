# Copy to `.env` in this directory:  cp .env.example .env
# Embedder API (sentence-transformers). Not committed.

EMBEDDING_MODEL_ID=sentence-transformers/all-MiniLM-L6-v2
EMBEDDING_DIMENSION=384
EMBEDDER_HOST=0.0.0.0
EMBEDDER_PORT=8100
